import requests

def get_proxies():
    url = "https://api.proxyscrape.com/v2/?request=getproxies&protocol=http&timeout=5000&country=all"
    return [f"http://{p}" for p in requests.get(url).text.splitlines()]

ROTATING_PROXY_LIST = get_proxies()
